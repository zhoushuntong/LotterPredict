<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rigsec.lotteranalsis">
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.READ_LOGS" />

    <application
        android:name=".LotterApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.activitys.addLotteryActivity" />

        <!--腾讯Bugly-->
        <!-- 配置APP ID -->
      <!--  <meta-data
            android:name="BUGLY_APPID"
            android:value="0aac0b1fd6" />
        &lt;!&ndash; 配置APP版本号 &ndash;&gt;
        <meta-data
            android:name="BUGLY_APP_VERSION"
            android:value="3.100.1" />
        &lt;!&ndash; 配置APP渠道号 &ndash;&gt;
        <meta-data
            android:name="BUGLY_APP_CHANNEL"
            android:value="achannel" />
        &lt;!&ndash; 配置Bugly调试模式（true或者false）&ndash;&gt;
        <meta-data
            android:name="BUGLY_ENABLE_DEBUG"
            android:value="true" />-->
        <!--腾讯Bugly-->
    </application>

</manifest>